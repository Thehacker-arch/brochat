# Copy to .env.production before: npm run build && cargo tauri android build
# Required for Android/iOS — the WebView origin is NOT your server URL.
VITE_API_BASE_URL=https://brochat.duckdns.org
VITE_WS_BASE_URL=wss://brochat.duckdns.org
